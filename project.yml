name: RetroCam
options:
  bundleIdPrefix: com.kreatix
targets:
  RetroCam:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - RetroCamApp.swift
      - RetroCamView.swift
    settings:
      GENERATE_INFOPLIST_FILE: YES
